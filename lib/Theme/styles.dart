import 'package:flutter/material.dart';

abstract class Styles {
  static const h2 = TextStyle(
    fontSize: 16,
    fontWeight: FontWeight.w600,
  );
  static const filmsDate = TextStyle(
    color: Color(0xFF999999),
    fontSize: 14,
  );
  static const p = TextStyle(
    fontSize: 14,
  );
}